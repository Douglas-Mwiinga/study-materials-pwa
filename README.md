# React + Vite

This app uses Vite for development and build tooling, with React. All source files are now at the project root or in the ./src directory. To get started:

```bash
npm install
npm run dev
```

For more info, see the Vite and React documentation:
- [Vite](https://vitejs.dev/)
- [React](https://react.dev/)
